# ARLADKR n=32 本地性能优化与对照追踪（2026-08-22）

本文记录把 ARLADKR 在本地 n=32 `original` 正式采样下的延迟从 **26.7s 降到 ~9.0s**（低于同
harness PracticalADKR 的 10.6s）、n=64 从 42.8s 降到 **33.7s** 的全部代码修改、安全论证、
测试数据与复现方式。

**最终状态与提交索引**（全部在 origin/main）：

| 轮次 | 提交 | 内容 | 关键数字 |
| --- | --- | --- | --- |
| W1/W2/W2b | `1aea8d6` | leaf 构建批量化、解码批量子群检查、接收方批验证 | n=32 quorum 26.7→9.0s |
| W3a/W4 | `f015e5f` | dealer payload 代服、分波恢复、validator 预热、settle 250ms | 通信 51→35MB |
| W5 | `4d4757f` | Practical responder grace + 同步启动 | Practical 24/32→32/32 |
| 文档 | `fee43fc` | 本文档首版（后续更新仅保留本地） | — |
| 预热模式 | `7af844d` | `RLADKR_VALIDATOR_PREWARM` 三态 + payload 缓存 | n=64 A/B 见第二轮 |
| n=128 基准 | `da551a6` | `RLADKR_RUN_N128_LOCAL_BENCH` 基准 | catalog 验证 151 CPU-s |
| 第四轮 | `9b82130` | 冗余重编码消除、scratch 缓冲、worker 上限 8、预热边界 n≤128 full | n=64 41.8→33.7s |所有修改不改
变协议阈值、采样算法、密码学方程或 latency 统计口径。操作账本见
[AWS_EXPERIMENT_TRACKING.md](AWS_EXPERIMENT_TRACKING.md)；本文只覆盖本地 224 核共享主机
（AMD EPYC，loopback 严格 TCP，每进程 GOMAXPROCS=4 对齐 c7g.xlarge 4 vCPU）的测量。

## 目标与结果

用户目标：ARLADKR 与 PracticalADKR 延迟相当。验收 harness：`scripts/run_cv_cluster.sh`
（ARL）与 `experiments/practical-adkr/scripts/run_practical_multiprocess_n7.sh`（Practical），
n=32、f=10、单 epoch、单节点单进程。

| 轮次 | mean / quorum latency | 完成节点 | sent/recv 每节点 | 峰值 CPU |
| --- | ---: | ---: | ---: | ---: |
| ARL `original` 基线（优化前） | 26 748 / 26 694 ms | 31/32 | 51.0/50.6 MB | 83.4 核 |
| ARL `smoke` 对照（优化前） | 21 339 / 21 384 ms | 32/32 | 13.4/13.4 MB | 71.7 核 |
| Practical κ=3 基线（修复前） | 10 431 / 10 476 ms | 24/32（8 个 CompProve 失败） | 19.2/19.0 MB | 35.3 核 |
| **ARL `original` 优化后 r1** | **8 866 / 8 666 ms** | 29/32 | 35.1/33.1 MB | ~92 核 |
| **ARL `original` 优化后 r2** | **9 018 / 9 295 ms** | 22/32（quorum 22 满足） | 37.2/30.6 MB | — |
| **ARL `original` 优化后 r3** | **9 064 / 9 238 ms** | 23/32（quorum 22 满足） | 38.7/33.8 MB | 91.9 核 / RSS 峰值 7.1 GB |
| **Practical κ=3 修复后（32/32）** | **10 632 / 10 629 ms** | 32/32 all_success | 19.1/19.1 MB | — |

三轮优化后 quorum 延迟 8.67–9.29s，均唯一 consensus hash。分阶段（r1）：
leaf build 4 193→**935ms**、candidate formation 19 964→**4 928ms**、aggregate agreement 1 384ms、
recover shard 净耗时 ~950ms（10s responder grace 已按 `service_grace_adjusted` 口径扣除）。

## 修改内容

### W1：leaf 构建批量化（leaf 4.2s → 0.9s 的 dealer 侧部分）

文件：`core/cv_sapvss_lanes_v2.go`、`core/cv_sapvss_lane_network_v2.go`、`core/cv_parallel.go`。

- 旧路径对每个接收者做 ~185 次 big.Int 单标量乘且串行。新 `cvComputeReceiverLaneOfferV2`
  把同一接收者的全部 G 基标量（chunk coins、blinding coin、ownership 承诺、weighted digit
  nonce、evaluation）合并为一次 `BatchScalarMultiplicationG1`，接收者 pk 基标量合并为第二次
  批乘，digit 点改用 V1 已有的 `cvDigitPointTable` 预计算表，h 基乘保持单次。
- `BuildLeafMaterialV2` 接收者循环用新 `cvLeafBuildWorkers`（env
  `RLADKR_LEAF_BUILD_WORKERS`，默认 GOMAXPROCS，上限 4）并行。
- 随机数生成顺序与取值语义不变；**同随机数下新旧实现 canonical wire 字节完全一致**，由
  `TestCVReceiverLanesV2BatchedMatchesLegacyImplementation`（对照保留的旧路径参考实现）与
  `TestCVReceiverLanesV2BatchedOfferMatchesScalarReference`（逐点标量乘参考）双重保证。
- 单接收者 A/B 基准（`BenchmarkCVV2ReceiverLaneBatchedVsLegacy`）：20.4ms → 4.1ms（5x）。

### W2：解码路径批量子群检查（catalog 验证 3.69s → 2.65s）

文件：新增 `core/cv_point_subgroup_batch.go`；`cv_sapvss_codec.go`、`cv_sapvss_ack_v2.go`、
`cv_sapvss_core_v2.go`、`cv_sapvss_leaf_v2.go`、`cv_sapvss_m4.go`。

- CPU profile 显示验证管线 93% 时间在 gnark `SetBytes` 内部的逐点 `IsInSubGroup`（每次约一次
  全阶标量乘）。新增 `cvDecodeG1WireUnchecked`（完整复刻 gnark marshal 语义，仅去掉子群检查）
  与 `cvAssertG1SubgroupBatch`：随机系数线性组合后做**单次**阶检查。任何非子群点使组合脱离
  子群的概率至多 `len(points)/r`，对 wire 规模的批次可忽略；系数在点固定后抽取。
- 热点解码单元（lane offer、ownership proof、core proof、unsigned leaf 系数承诺）改为
  `pointDeferred/ciphertextDeferred` 收集，单元结束统一 `assertDecodedSubgroup()`；非热点路径
  保持原有立即检查。
- 安全测试 `TestCVSubgroupBatchRejectsNonSubgroupPoints`：随机采样曲线上子群外的点必须被
  批检查拒绝（且 gnark 参考实现同样拒绝），合法点批通过。
- `BenchmarkCVV2ProposerCatalogVerifyN32`：3 687ms → 2 637ms（22 组件、4 worker）。剩余大头
  为压缩点解压的 sqrt（不可再减，除非改非压缩 wire——会加倍通信量，已否决）。

### W2b：接收方 ACK ownership 批验证

文件：`core/cv_sapvss_lanes_v2.go`（`cvVerifyAndDecryptReceiverLanesModeV2`）。

- 接收方处理 lane offer 时从逐方程 exact 验证（~60ms/offer）改为对单 offer 调用既有
  `cvVerifyOwnershipBatchV2`（67 个方程一次 MSM，失败自动回退逐接收者 exact），接受语义不变。

### W3a：dealer payload 认证代服 + 恢复分波（通信 51 → 35MB）

文件：`core/cv_sapvss_apdb.go`（payload 响应 codec）、`cv_sapvss_apdb_recovery.go`（collector
快速路径）、`cv_sapvss_apdb_network_v2.go`（服务端）、`cv_sapvss_component_network_v2.go`
（发布缓存）、`cv_sapvss_router.go`（新 tag）。

- 新消息 `CV_V2_APDB_RECOVER_PAYLOAD`：dealer 在发布组件时缓存自身 payload；收到组件恢复
  请求时优先以完整 payload 应答。消费方 `AddPayload` 用**确定性重编码 + Merkle root 与 lock
  证书比对**认证——与分片恢复路径完全同等的绑定，不引入新的信任假设；`bindingCheck` 照常执行。
- 恢复请求改为 dealer 优先 + 请求方轮转的前 `dataShards+3` 个持有者（
  `cvRecoveryFirstWaveV2`），500ms 未凑齐再扩散到全员（`cvSendRecoveryRequestsWithWavesV2`），
  重试上限不变。liveness 语义不变（全员可达路径保留）。
- 效果：消除 32 持有者对每个请求的全员分片应答突发，恢复延迟方差显著下降（n=32 曾因此把
  ~9 个节点拖进 MVBA 尾部）。

### W4：validator 预热 + ACK settle 默认 250ms

文件：`core/cv_sapvss_apdb_network_v2.go`（`setEligibilityCoin`）、`core/cv_parallel.go`。

- eligibility coin 确定后，被采样的 **validator**（原仅 proposer）也启动 catalog 预热；与
  proposer 共享同一 `verifiedComponentsV2` 缓存与 single-flight，validation request 到达时多为
  缓存命中，validator 恢复（原最高 27.9s）移出候选后关键路径。
- `cvDefaultACKSettleGraceV2` 25ms → 250ms：旧默认下每节点 ~8 个接收者被 25ms 窗口挤成
  fallback 证据路径（构建与验证均未批化，秒级成本）。250ms 后 `mean_apvss_fallback_count`
  7.9 → **0**。env `RLADKR_ACK_SETTLE_GRACE_MS` 仍可覆盖（上限 250）。

### W5：Practical responder 有界 grace + 同步启动（对照公平性）

文件：`experiments/practical-adkr/core/comp_multicast.go`、`core/adkr.go`、
`cmd/bench_latency/main.go`、`scripts/run_practical_multiprocess_n7.sh`。

- 已知尾部竞态（AWS 2026-08-21 记录）：成功节点退出后关闭 CompProve responder，慢节点
  `reachable≤8 need=22` 超时。新增 `PRACTICAL_RESPONDER_GRACE_MS`（默认 0 保持旧行为）：
  成功 epoch 后有界延迟关闭 responder；失败路径立即关闭。
- `PRACTICAL_START_AT_UNIX`（脚本默认 now+8s）：全部节点同一墙钟启动，消除共享主机 spawn
  偏斜对接续 readiness 窗口的挤压。
- 本地 n=64 无需更改即可受益；n=32 实测 24/32 → **32/32 all_success**（grace 75s）。

### harness：`scripts/run_cv_cluster.sh` 支持 `-cv-failure-target` 透传

`RLADKR_CV_FAILURE_TARGET`（默认 `smoke` 保持历史行为）传入 rladkrbench，正式采样口径不再
需要手工补丁。

## 本地测试数据明细

全部为 224 核 AMD EPYC、275GiB、loopback 严格 TCP、GOMAXPROCS=4/进程；产物在运行主机
`/tmp/n32-local/`（logs、cluster-results、监控文件）。采样口径（`cmd/cvv2ref -manifest-only`）：

| n/f | profile | proposer | validator（阈值） | 说明 |
| --- | --- | ---: | ---: | --- |
| 32/10 | smoke | 3 | 3（2） | 流程验证，无安全预算 |
| 32/10 | original | 11 | 21（11） | Δ=1e-10，逐项界=0 |
| 64/21 | original | 16 | 43（22） | 同上 |

优化过程关键中间轮（n=32 original）：W1+W2 → 25 799ms（22/32）；+W2b → 24 851ms（23/32）；
+W3a → 23 146ms（25/32，sent 39.5MB）；+W4+settle250+lane4 → **8 866ms**（29/32）。W4 轮
fallback 0 与 dealer 代服是两个最大跳变。

ARL 尾部 22–29/32 波动属 n-f 语义：晚进 MVBA 的节点在快节点退出后无法凑齐 22 个存活参与者，
三轮 quorum 均满足；AWS runner 的成功判据同样是 n-f。Practical 32/32 是 responder grace 的
结果，两者口径在 quorum 层面可比。

## n=64 扩展性数据（original，2026-08-22 追加）

n=64、f=21、`original`（proposer 16、validator 43/阈值 22、pool 43 组件）、其余 harness 同上。
**注意：64 进程 × GOMAXPROCS 4 = 256 线程，超出 224 核主机约 14%，CPU 峰值 214 核已饱和，
下列数字受共享主机超订影响，偏保守。**

| 指标 | 数值 |
| --- | ---: |
| mean / quorum latency | 42 567 / 42 800 ms |
| 完成节点 | 45/64（quorum 43 满足，consensus 唯一） |
| leaf build | 2 173 ms |
| candidate formation / proposer slots | 32 427 / 32 022 ms |
| proposer catalog verify / recovery | 13 938 / 10 422 ms |
| validator component recovery（预热后） | 14 115 ms（max 37 364） |
| aggregate agreement | 4 037 ms |
| recover shard（含 10s grace） | 12 154 ms |
| fallback count | 0.7 |
| sent / recv 每节点 | 162.1 / 136.8 MB |
| 峰值 CPU / RSS | 214 核 / 30.8 GB |

n=64 的主要瓶颈与 n=32 不同：validator 样本 43/64 意味着几乎全体预热 43 个组件（CPU 与
通信都 ~n² 级），主机饱和后 catalog verify（13.9s）与恢复（10–14s）同时膨胀。后续方向：
跨组件批验证合并（一次 MSM 覆盖 43 个组件的方程）、W3b 分用途 shard 尺寸（162MB 通信的主要
来源是 aggregate 分散与全员预热的组件传输）、以及 validator 预热降级为"恢复不验证、请求到达
再验证选中的 22 个"。在拿到 n=64 专用集群（≥256 核）前，不应把这组数字当作论文正式点。

## validator 预热模式与通信构成（2026-08-22 第二轮追加）

n=64 的 162MB/节点通信分解显示 **86% 在 candidate 阶段（139MB sent）**：组件分散仅 7.6MB、
recover shard 5.8MB。大头是 validator 全目录预热的 payload 拉取（43×0.92MB/节点）与 dealer
对每个请求者的 payload 代服（~54MB/dealer）。因此：

- **W3b（分用途 shard 尺寸）经数据证伪为主因**：它针对的组件分散/分片传输只占 ~8%，搁置。
- **跨组件 MSM 合并经 profile 证伪为低收益**：验证热点是压缩点解压 sqrt（逐点不可合并），
  MSM 仅占小头，搁置。
- 真正有效的是 **validator 预热模式**：新增 `RLADKR_VALIDATOR_PREWARM`
  （`recover`/`full`/`off`，尺寸感知默认：n≤32 为 `full`，n>32 为 `recover`）。`recover` 只
  预拉 payload（进入 `recoveredPayloadsV2` 缓存 + single-flight），验证延迟到 validation
  request 选中的组件，validator CPU 从"验证全 pool"降为"验证选中集合"。

n=64 A/B（其余参数同上表）：

| 模式 | quorum latency | 完成节点 | sent/recv 每节点 | 结论 |
| --- | ---: | ---: | ---: | --- |
| full（旧行为） | 42 800 ms | 45/64 | 162.1/136.8 MB | 延迟最低、CPU/字节最高 |
| **recover（n>32 默认）** | 45 619 ms（+6.5%） | 47/64 | **135.1/120.4 MB（−17%）** | 规模优先 |
| off | 456 348 ms | 51/64 | 158.7/147.4 MB | **禁止**：验证请求风暴触发按需恢复，dealer 饱和后 500ms 宽限到期转为全员分片扇出，雪崩 |

n=32 复核（尺寸感知默认=full）：quorum 9 315ms、26/32、共识唯一——与优化后基线一致。
注：期间观察到一轮 0/32 全失败，根因是既有的 decision finalization 尾部活性级联（某节点
先失联，其余节点 21/22 凑不齐阈值集体报错），复跑即恢复，非本次改动引入；放大规模前建议
给 FinalizeDecision 的有界重试同样加 responder service grace（与 W5 同型修复）。

## n=64 干净环境复测与 n=128 阶梯结论（2026-08-22 第三轮追加）

> 注：本节的"n>32 默认 recover"在第四轮改为"n≤128 默认 full"，以本节为历史记录。

宿主机重启清空 /tmp 后，在干净 224 核环境用当前代码（`7af844d`，n>32 默认 `recover`）复测
n=64 original：

| 指标 | 数值 |
| --- | ---: |
| mean / quorum latency | 42 130 / **41 752 ms** |
| 完成节点 | **56/64**（历史最好；此前 full 45、recover 47） |
| leaf build / candidate formation / agreement | 2 151 / 33 158 / 3 364 ms |
| proposer catalog verify / recovery | 4 469 / 3 362 ms（proposer slots 32 910 ms，余下为聚合锁定与 VCert 等待） |
| validator recovery（recover 预热后） | 9 524 ms |
| sent / recv 每节点 | **126.99 / 120.27 MB**（新低） |
| 峰值 CPU / RSS | ~99.7 核 / 30.9 GB |
| fallback count | 0.6 |

**n=128 阶梯测试结论**（GOMAXPROCS=1、epoch 900s 的 smoke 全轮 128 节点超时；分阶段基准）：

- leaf build（128 接收者，串行）：966 ms —— 可扩展。
- proposer catalog 验证（86 组件、4 worker）：**37.9s 墙钟 ≈ 151 CPU-s/proposer**——随
  n² 增长（组件数 × 每组件点数），是 n=128 的主导成本。
- n=128 单核/节点在 900s 内完不成 proposer 路径（151s 纯验证 + 恢复 + 聚合锁定 + VCert）；
  属算力不足超时而非死锁。本地全量验证需 ≥2 核/节点 + epoch ≥1800s（256 线程对 224 核
  超订 ~14%），预计 proposer 路径 ~300-500s；按 AWS c7g.xlarge（4 vCPU 专用/节点）推算
  catalog 验证 ~38-60s/proposer，`recover` 模式下 600-900s epoch 应当可行。
- n=128 基准入口：`RLADKR_RUN_N128_LOCAL_BENCH=1 go test ./core -run '^$' -bench
  '^BenchmarkCVV2LeafBuildN128$|^BenchmarkCVV2ProposerCatalogVerifyN128$'`。

## 第四轮：消除冗余重编码 + 预算/预热调整（2026-08-22，提交 9b82130）

n=128 catalog 验证 profile（86 组件、62.5 CPU-s/迭代）：**sqrt 解压 40s（64%，压缩 wire 的
硬地板）**，非 sqrt 开销 22.4s（36%），其中大头是每个 leaf 解码后的 2–3 次 1.7MB 规范重编码
（dealer 签名 statement、digest 校验）与每点 48B 堆分配（12.2GB/迭代）。

改动（均不改协议结构与语义）：
- `cvDecodeLeafV2` 在已验证 wire 规范性后，直接从 wire 的 unsigned 子切片计算 dealer 签名
  statement 哈希，并携带 canonical-verified 标记；`cvVerifyAPVSSModeV2` 复用它们，跳过两次
  全量重编码（本地构建的 leaf 走原路径）。digest 校验在 canonical==wire 下恒真，安全等价。
- wireReader 复用 scratch 缓冲读压缩点，消除每点分配。
- `cvLeafVerifyWorkers` 上限 4→8（8 vCPU 实例不再被卡）；validator 预热默认边界改为
  n≤128 用 `full`（proposer catalog 阶段 validator 核空闲，后台预热验证正好填补），n>128
  仍为 `recover`。

量化：n=32 catalog 基准 2.637→2.567s、allocs −21%、bytes −25%；**n=64 original（full 默认）
quorum 41.8s → 33.7s（−19%）、57/64 完成、共识唯一**（同一 recover 对照轮 45.3s/43 节点，
干净基线 41.75s/56 节点）。

**n=128 外推**（按基准与 n=64 分解，未实测）：4 vCPU/节点下 proposer 路径 ≈ catalog 验证
12.9s（86×0.60 CPU-s÷4）+ 恢复 ~10s + pool/聚合锁/VCert ~8s + leaf 1s + agreement ~5s +
recover ~2s ≈ **38–40s**；**8 vCPU（c7g.2xlarge，worker 上限已放开）下验证降至 ~6.5s，
合计 ≈ 30–32s，达到 30s 目标边界**。sqrt 地板决定了 4 vCPU 无法到 30s；若论文实验在 n=128
使用 2xlarge 实例即可满足。遗留可再压项：pool/聚合锁/VCert 的 ~8s（n=64 中 candidate 阶段
非 catalog 部分）尚未分解优化。

## 第五轮：dealer payload 双形式点附件（2026-08-22，本地未提交）

第四轮确认 sqrt 解压是压缩 wire 的地板（n=128 占 64%）并据此判定 4 vCPU 无法到 30s。该否决
针对的是**纠删分散 wire 改非压缩（全员 n² 字节翻倍）**；W3a 之后组件获取的主路径已是 dealer
单播代服，在这个路径上用字节换 sqrt 不改变全员分散的通信量。本轮据此实现**双形式点附件**：

- 机制：`CV_V2_APDB_RECOVER_PAYLOAD` 响应可选携带一段 96B/点的非压缩附件（新文件
  `core/cv_point_hints.go`），覆盖 leaf 解码中**按 wire 顺序**经 `pointDeferred` 读取的全部
  点（offer/ownership/core proof/系数承诺）。消费侧对每个附件点做**显式 on-curve 检查 +
  重压缩逐字节比对** canonical wire——重压缩与解压互为确定逆映射，因此附件不可能引入签名
  payload 之外的任何点；任何短缺、损坏、曲线不符都使该次解码**永久回退 sqrt 路径**，正确性
  不变、仅损失性能。子群批检查（W2）照常覆盖附件提供的点。
- 生成：dealer 在缓存 payload 后**懒生成**（首次收到恢复请求时 once.Do 一次全量解码记录，
  复用消费侧同一代码路径保证顺序一致），之后对所有请求者复用。
- 穿透：sidechannel 经 `cvDecodeLeafV2Unsigned/core proof/lane offer/ownership/ACK evidence`
  五层嵌套解码传递；恢复链 `collector→RecoveredHints→recoverComponentForPurpose→
  recoveredPayloadsV2 缓存→verify/verifiedComponentLeafV2` 全程透传，proposer catalog 管线
  与 validator full 预热同样受益。env `RLADKR_APDB_PAYLOAD_HINTS`（当前默认关；显式 `1` 同时开服务与
  使用）。旧格式响应（无附件字段）双向兼容。
- **顺带收紧一个 W2 缺口**：`cvDecodeG1WireUnchecked` 的非压缩 mask 分支原本不带 on-curve
  检查（压缩分支由 sqrt 成功隐含），现已补上；W2 的批子群论证从此对两种 mask 都成立。

测试（`core/cv_point_hints_test.go`，全绿）：记录/回环等价（digest 一致、可验证）；六种损坏
（翻字节/截断/延长/交换/空流/尺寸错位）全部回退且结果不变；重压缩与 gnark 序列化逐字节一致
（含无穷远点、取负不等）；**同奇偶异 y 的离曲线注入被 on-curve 检查拒绝并回退**；忠实镜像
非子群点的附件仍被批子群检查拒绝（安全语义不变）；响应 wire 回环 + 超界（2×maximumPayload）
与错位拒绝。

基准（同夹具 A/B、4 worker、3 次均值）：

| 基准 | legacy | hints | 提升 |
| --- | ---: | ---: | ---: |
| n=32 catalog（22 组件） | 2 573 ms | 1 917 ms | **−25.5%** |
| n=64 catalog（43 组件） | 9 463 ms | 7 065 ms | **−25.3%** |

n=64 每组件 payload 919KB、hints 1 243KB（12 950 点），dealer 单播响应 0.92→2.16MB（×2.35）。
**解读**：n=32/64 上第四轮后的 sqrt 份额实测约 25%（n=128 profile 的 64% 是更大 leaf 的构成，
点数随 n² 增长而 MSM/重编码部分份额下降），因此集群级收益预计与基准同量级——n=64 干净基线
的 proposer catalog verify 4.47s 预计降至 ~3.3s，validator full 预热同幅减压（对 4 vCPU 争用
与预热窗口都有利）。**通信代价**：candidate 阶段 dealer 代服字节 ×2.35，n=64 每节点 payload
拉取 40MB→~93MB，总 sent 预计 127→~180MB（+40% 量级）；正式数字待 n=64 集群复测（见遗留）。
全量 `go test ./core -count=1` 213s 全绿（与基线一致）。

**批配对评估（本轮第二项，结论搁置）**：核实 `cvVerifyValidationCertificateV2` 已是聚合式验证
（G2 公钥求和 + 单次配对），配对热点仅在 `cvBuildValidationCertificateV2` 等**收集阶段**的逐
部分签名验证（n=128 时 43 次/VCert）；随机系数批量配对可省约百毫秒级/节点，复杂度收益比不
划算，搁置并记录。

### 第五轮 n=64 集群验证（2026-08-22 本地实测）

同一 224 核宿主、loopback 严格 TCP、GOMAXPROCS=4/进程、`original` 正式采样口径，三组各单轮：

| 组 | quorum latency | 完成节点 | sent/recv 每节点 | 共识 |
| --- | ---: | ---: | ---: | --- |
| **ARL hints 开（默认）** | **31 518 ms** | 49/64（quorum 43 满足） | 186.7 / 159.2 MB | 唯一 |
| ARL hints=0（同代码基线） | 33 628 ms | 43/64（quorum 恰满足） | 144.8 / 118.8 MB | 唯一 |
| Practical κ=3 | 42 093 ms | 53/64（quorum 满足，max 尝试 87.1s） | 148.1 / 147.6 MB | 唯一 |

- **hints 净效应（同代码 A/B）**：quorum 33.6→31.5s（−6.3%），sent +29%（144.8→186.7MB）、
  recv +34%。catalog CPU 的 −25% 传导到端到端为 −6.3%，符合 catalog verify（~15s，本轮含
  争用）只占 proposer 路径一部分的分解；hints-off 轮 33.6s 与第四轮 33.7s 高度吻合（可复现）。
- **ARL vs Practical（quorum 口径）**：31.5s vs 42.1s，**ARL 快 25%**（n=32 时为 9.0 vs 10.6s、
  快 15%）——规模越大差距越开。
- **通信比剧变**：n=64 上 ARL/Practical sent = 186.7/148.1MB ≈ **1.26x**（n=32 时为 ~1.8x，
  更早口径 ~7x 是 Practical 19MB 时代）。Practical 从 n=32→64 通信 ×7.7（19.2→148.1MB），
  ARL ×5.3（35→187MB）；第五轮 hints 的 +42MB 是主动取舍，若关 hints 则为 144.8MB（<1x
  Practical）。
- 分阶段（hints 轮，采样节点）：leaf build 2.0–2.5s、candidate formation 21.8–22.5s（其中
  catalog verify 15.0–15.3s、组件恢复 recv 76–79MB/发起者）、agreement 3.6s、recover shard
  11.7s、fallback 0。本轮共享主机争用高于第三轮干净复测（catalog 15s vs 4.5s），单轮数据
  偏保守。
- 注意 Practical 仍为 κ=3 显式档（每 epoch 失败概率 0.108，弱安全对照），matched-lifetime
  口径下其延迟会更高。

## 第六轮：Practical δ good-case 复现 + ARL FinalizeDecision 活性修复（2026-08-22，本地未提交）

**ARL FinalizeDecision 有界重试 + responder grace（AWS 前必做项，已完成）**：0/32 尾部级联的
根因是重试预算过小（4 次尝试 ≈5.5s 退避窗口），CPU 争用下 21–22 个存活节点来不及交换
decision share 即集体报错。`FinalizeDecision` 改为**墙钟预算制**（`RLADKR_DECISION_RETRY_
BUDGET_MS`，默认 30s、上限 120s；ctx 取消仍立即中止；阈值/密码学不变），成功路径的收尾
linger 支持 `RLADKR_DECISION_RESPONDER_GRACE_MS`（默认 0 保持现状，AWS 可放宽；linger 照旧
不计入报告延迟，失败路径不 linger）。

**Practical δ good-case（论文 Fig 4c/4d，实现完成、对照未通过，调查中）**：
`PRACTICAL_DEALING_DELTA_MS`（dealing 阈值后继续收集 δ；论文值 n≤64 用 2s、n≥127 用 1s）与
`PRACTICAL_DERIVE_WAIT_ALL_MS`（KEY 派生阈值后等待全员）两个旋钮，默认 0 = 遗留行为。调试过
程发现并修复了复现版 CompProve/DXT 的**两个真实竞态**：① wait-all 窗口内 collector 把后续
阶段的 completion 消息从共享 channel 吃掉（修复：暂存并在退出时回注）；② δ 窗口跨 dealer
不同步导致"transcript 含签名但本地 aux 快照未含份额"（修复：derive 阶段从活跃 receiver
服务重新快照，MVBA 定案后必然安全）。修复后 n=7 可过（5–7/7），但 **n=64 仍失败于第三类
问题**（"CompProve completion input incomplete"，同族相位问题的又一处），δ 对照数据尚无效；
期间还记录了宿主 n=7 的**本底间歇抖动**（原生代码 0/7×3 后 6/7×1，"复跑即恢复"家族，与 δ
无关——判定方法：git stash 后跑原生对照）。结论：δ 实现保留但默认关，n=64 对照待第三处
修复后重跑；顺带把 compProof 的 aux 错误拆分为 missing-entry/commitment-mismatch 两类便于
后续诊断。

### 第六轮四组本地 TCP 复测（2026-08-22，当前代码、单轮）

同宿主与口径（loopback 严格 TCP、GOMAXPROCS=4、original 正式采样；ARL hints 默认开、
Practical δ 默认关=遗留行为，含两个竞态修复）：

| 测试 | quorum latency | 完成节点 | sent/recv 每节点 | 共识 |
| --- | ---: | ---: | ---: | --- |
| ARL n=32 | **8 973 ms** | 26/32（quorum 22 满足） | 51.8 / 47.8 MB | 唯一 |
| Practical n=32 | 10 384 ms | 29/32 | 19.2 / 19.1 MB | 唯一 |
| ARL n=64 | **31 450 ms** | 54/64（quorum 43 满足） | 211.8 / 191.2 MB | 唯一 |
| Practical n=64 | 42 025 ms | **64/64 all_success** | 147.9 / 147.7 MB | 唯一 |

ARL 两个规模均领先（n=32 −14%、n=64 −25%）；n=32 quorum 落在历史带 8.67–9.29s 内、
n=64 与第五轮 hints 结果一致（31.5s），第六轮活性改动无回归。Practical n=64 本轮首次
64/64 all_success（grace 120s），42.0s 与此前 42.1s 吻合；两个竞态修复对 δ-off 行为
无扰动。通信：ARL n=32 的 51.8MB 含 hints 代价（基线 ~35–38MB）；Practical n=32→64
通信 ×7.7（19.2→147.9MB）vs ARL ×4.1（关 hints 口径 51.8→144.8MB）。

## 复现命令

```bash
# ARL n=32 original（正式采样）
RLADKR_CV_GOMAXPROCS=4 RLADKR_CV_EPOCH_TIMEOUT=600s RLADKR_CV_RUNNER_TIMEOUT=900s \
RLADKR_CV_QUORUM_SETTLE_TIMEOUT=45s RLADKR_LANE_WORKERS=4 RLADKR_CV_FAILURE_TARGET=original \
bash scripts/run_cv_cluster.sh 32 10 /tmp/arl-n32 20000

# ARL n=64/128 附加旋钮
#   RLADKR_VALIDATOR_PREWARM = full|recover|off（默认 n≤128 full、n>128 recover；off 禁用）
#   RLADKR_LEAF_VERIFY_WORKERS 上限 8：n=128 建议 8 vCPU 实例（c7g.2xlarge）
#   RLADKR_APDB_PAYLOAD_HINTS = 0 可整体关闭第五轮双形式附件（服务与使用两侧）
#   分阶段基准：RLADKR_RUN_N32_LOCAL_BENCH=1 / RLADKR_RUN_N64_LOCAL_BENCH=1 / RLADKR_RUN_N128_LOCAL_BENCH=1

# Practical n=32（κ=3 显式；grace/同步启动已内置脚本）
GOMAXPROCS=4 PRACTICAL_MP_N=32 PRACTICAL_MP_F=10 PRACTICAL_MP_KAPPA=3 \
PRACTICAL_MP_TIMEOUT=900s PRACTICAL_RESPONDER_GRACE_MS=75000 \
KEEP_PRACTICAL_MP_RUN=1 bash experiments/practical-adkr/scripts/run_practical_multiprocess_n7.sh
```

测试与基准：`go test ./core -count=1`（全绿，213s）；
`RLADKR_RUN_N32_LOCAL_BENCH=1 go test ./core -run '^$' -bench
'^BenchmarkCVV2ProposerCatalogVerifyN32$|^BenchmarkCVV2LeafBuildN32$' -benchtime=3x`；
n=64 catalog A/B（同一夹具 legacy/hints）：
`RLADKR_RUN_N64_LOCAL_BENCH=1 go test ./core/ -run '^$' -bench
'^BenchmarkCVV2ProposerCatalogVerifyN64$' -benchtime=3x`。

## 遗留与后续

- **第五轮 n=64 集群复测**：已完成（见第五轮小节）：quorum −6.3%、sent +29%；若论文口径
  通信预算紧张，可用 `RLADKR_APDB_PAYLOAD_HINTS=0` 换回 −42MB/节点（延迟 +2.1s）。
- **已证伪搁置**：W3b 分用途 shard 尺寸（组件分散仅占 n=64 通信 ~8%，非主因）；跨组件 MSM
  合并（验证热点为逐点 sqrt 解压，占 64%，不可合并）；部分签名收集的批量配对（聚合验证已是
  单配对，收集处收益仅百毫秒级）。
- **candidate 阶段非 catalog 部分**（n=64 约 ~8s：pool 证书、aggregate APDB 锁、VCert 收集）
  尚未分解优化，是 8 vCPU 下从 30s 再压到 ~25s 的主要空间。
- **decision finalization 尾部级联**：已修复（第六轮，预算制重试 + 可选 responder grace）；
  AWS 复测时建议显式设 `RLADKR_DECISION_RESPONDER_GRACE_MS=30000`。
- **Practical δ 第三处竞态**："CompProve completion input incomplete"（n=64、δ 开启时全节点
  失败），与已修复的两处同族（共享 channel 相位重叠），待定位后重跑 n=64 δ 对照。
- **n=128 实例规格**：4 vCPU 物理天花板约 38–40s（sqrt 地板）；30s 目标需 8 vCPU
  （c7g.2xlarge，worker 上限已放开）。第五轮附件直接削减该地板（n=128 点数最多、收益占比
  最高），4 vCPU 外推需按附件实测重估。本地全量验证需 ≥2 核/节点 + epoch ≥1800s。
- **预热边界复测**：第五轮同时降低 proposer catalog 与 validator full 预热的 CPU，n≤128 用
  full 的空闲窗口假设变化，放大规模前应重测 full/recover 边界（此前已两次调整，对负载形状
  敏感）。
- 通信量仍约为 Practical 的 ~7x（n=64：127–140MB vs 19MB；ARL 聚合对象公开可验证的额外
  密码材料 + dealer 代服所致），延迟目标已达成；第五轮附件会使该比值进一步上升 ~40%，论文
  叙述时应明确"字节换验证 CPU"的取舍。
- AWS 复测：无需重 bake AMI（binary staging 分发当前构建）；部署配置建议显式
  `RLADKR_LANE_WORKERS=4`（n=128 用 8）；ACK settle 250ms 已为默认。

## 第七轮：跨 Region quorum 收集与 candidate 控制面 profile（本地实现）

### AWS 编排

- cross-region suite 的 regional Terraform apply、协议 ingress 二次 apply 和 destroy 改为并发；
  区域仍共享同一 NodeSlot roster，任何区域失败仍使整轮失败。
- quorum 完成后，收集器先读取所有节点的 run status，按 NodeSlot 选择确定性的 `n-f` 个成功节点，
  只收集这些节点的 summary。未选节点保留在 manifest 的 `uncollected_hosts` 中，不影响有效 quorum
  数据判定；full diagnostics 仍可显式收集。
- 代码和离线 Fabric 测试已完成；真实跨 Region SSM、公网 roster 和 Spot interruption 仍需小规模
  AWS smoke 验证，不能把本轮变更写成已完成的 AWS 实测收益。

### VCert profile 与控制面优化

新增并输出以下分段指标：

- `mean_vcert_canonical_ms`：validation request canonical encode；
- `mean_vcert_network_wait_ms`：sample validator request 到 threshold response 的等待；
- `mean_vcert_signature_verify_ms`：validator handler 对部分签名的 pairing 验证；
- `mean_vcert_aggregate_verify_ms`：构造 VCert 后的单次聚合 pairing 验证。

已完成的低风险改动：

- validation request 只 fanout 到 `validatorSample`，不再向整个 old roster 发送无效请求；
- proposer 本地 request 保留 canonical wire，跳过立即重复 decode；
- 构造 VCert 时删除已在 handler 完成的逐签名二次 pairing，最终 aggregate pairing 仍保留；
- Aggregate APDB lock 在 threshold ready 后取消尾部 offer 调度，不再等待所有 holder send 返回；
- `RecoverLock` 只取 roster 顺序中的 threshold shares，避免处理多余 shares。

这轮尚未提交 AWS 实测数据。batch pairing 暂不单独实现：部分签名已在接收 handler 逐个验证，构造
阶段的重复 pairing 已删除，最终证书验证已经是一次 aggregate pairing；先用新 profile 判断剩余
`signature_verify` 与 `aggregate_verify` 的真实占比，再决定是否值得引入更复杂的批配对协议。
- Practical 的 κ=3 显式档每 epoch 仅 ~3.5 bit 安全性、lifetime 0 bit；论文对照应使用
  matched-lifetime κ 重跑（其延迟会上升，对 ARL 更有利）。

## 第八轮：dealer/receiver recovery 热路径解耦与 proposer 分工互验证书设计（2026-08-23）

### 已实现的实现层优化

本轮先处理 component recovery 的请求路径，不改变 APDB wire、阈值或认证语义：

- `APDBRecoverGet` 和完整 payload response 在 inbox 中只入队；dealer hints 记录、完整
  response canonical encode、receiver payload decode/root re-encode 由独立 recovery worker
  pool 执行，避免大 payload 工作阻塞控制面 dispatch。
- dealer 在 payload cache 后预排 response preparation；同一 instance 的完整
  `payload+hints` response wire 只 encode 一次，后续请求复用。`RLADKR_APDB_PAYLOAD_HINTS=0`
  仍禁用 hints，但不回退到逐 holder shard burst。
- 默认 recovery service worker 为 `max(2, committee/16)`、上限 16；
  `RLADKR_APDB_RECOVERY_WORKERS` 可设 `4/8/12/16`。它与
  `RLADKR_COMPONENT_RECOVERY_WORKERS` 分离，前者限制服务请求处理，后者限制 proposer
  recovery fanout。
- 新增 profile：`mean_dealer_hint_build_ms`、`mean_dealer_response_encode_ms`、
  `mean_receiver_payload_validation_ms`、`mean_recovery_queue_wait_ms`、
  `mean_recovery_worker_ms`。queue wait、worker 处理和端到端 recovery latency 是不同口径，
  不能相加。

本地定向 `go test ./core ./cmd/rladkrbench` 和 recovery/network 相关 `-race` 测试通过；
尚无 AWS 数据，新增成本 `$0`。本地 n=64/n=96 固定-topology worker A/B 已完成，结果如下。

### recovery workers 4/8/12/16 A/B（2026-08-23）

八档均在同一 224-vCPU 主机顺序运行；每个规模内部固定严格 loopback TCP topology、全量 listener
barrier、smoke sampling、`GOMAXPROCS=4`、leaf verify workers=4 和 component recovery
workers=8，仅改变 `RLADKR_APDB_RECOVERY_WORKERS`。RSS 每 500 ms 采样，GC 使用
`GODEBUG=gctrace=1`。每档为单 epoch，因此用于并发选型，不作为论文统计数据。

| n | workers | 完成 | E2E 均值 | queue wait | worker wall | sent/recv 每节点 | 簇峰值 RSS | GC live peak 均值/最大 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 64 | 4 | 63/64 | 18.99 s | 139.73 ms | 1489.83 ms | 30.93/26.95 MB* | 13.02 GiB | 290/600 MB |
| 64 | **8** | **64/64** | **17.64 s** | 135.45 ms | 1296.33 ms | 31.13/31.11 MB | 11.23 GiB | 287/412 MB |
| 64 | 12 | 62/64 | 18.03 s | 115.66 ms | 1353.54 ms | 30.76/31.56 MB* | 12.48 GiB | 288/437 MB |
| 64 | 16 | 63/64 | 17.33 s | 89.26 ms | 1219.52 ms | 29.58/27.09 MB* | 9.00 GiB | 278/438 MB |
| 96 | 4 | 96/96 | 43.05 s | 84.27 ms | 2080.69 ms | 43.96/43.95 MB | 19.25 GiB | 406/686 MB |
| 96 | 8 | 96/96 | 42.47 s | 73.34 ms | 2180.46 ms | 44.46/44.45 MB | 19.67 GiB | 405/703 MB |
| 96 | **12** | **96/96** | **42.05 s** | **57.56 ms** | 2121.65 ms | 42.71/42.69 MB | 19.72 GiB | 408/696 MB |
| 96 | 16 | 95/96 | 45.74 s | 70.29 ms | 2408.77 ms | 43.50/39.28 MB* | 24.67 GiB | 427/780 MB |

`*` 的 recv 均值被未完成节点截断，不能视作节省通信。完整轮通信仅有约 4% 单轮调度波动；worker
旋钮不改变 wire。GC 平均累计 clock 在 n=64 为 192--208 ms、n=96 为 313--373 ms，不是主要
瓶颈；n=96/w16 的 live heap 与 RSS 均最高。结论是保留当前规模自适应公式：n=64 默认 8 是唯一
all-success 档，n=96 默认 12 是全完成档中最快且 queue wait 最低。固定 16 在 n=96 反而慢
3.69 s、丢一个尾节点并增加约 25% 簇峰值 RSS。原始产物位于
`/tmp/arladkr-recovery-workers-ab-20260823/`。

### n=128 严格 TCP smoke（2026-08-23）

同一主机以 `n=128,f=42`、全量 listener、smoke sample=3/3、`GOMAXPROCS=4`、leaf workers=4、
component recovery workers=8、默认 APDB recovery workers=16 跑一个 epoch。结果为 `127/128`
成功、quorum 86、单一共识 hash；adjusted E2E `92.52 s`，candidate/proposer slots
`67.68/67.43 s`，agreement `3.50 s`，recover shard `15.45 s`（含约 10 s grace）。4 个实际
proposer 的 catalog verify 为 `54.1--57.1 s`。每成功节点 sent/recv `51.27/46.01 MB`（recv
缺少一个尾节点），簇 RSS 峰值 `52.33 GiB`、单进程采样峰值 `1464.7 MiB`、GC live peak
均值/最大 `576/1120 MB`，GC clock 每节点累计均值约 `0.77 s`。

本机可以完成 n=128，但 `128*GOMAXPROCS(4)=512` 对 224 CPU 为 2.29 倍超配；node 69 在 20 s
settle 窗口内仍处于 GC/计算尾部而被 harness 清理，没有协议错误。因此 92.52 s 是共享主机上界，
不等价于 128 台 4-vCPU AWS。原始产物在 `/tmp/arladkr-n128-local-w16-20260823/`。

按当前门控基准和历史 AWS 数据，128 台 `c7g.xlarge` 同 AZ 私网 smoke 预期 `40--65 s`、约
`50--65 MB/节点`；美爱两区各 64 台公网 smoke 在 MVBA 健康时预期 `120--300 s`。正式 original
为 proposer=19、validator=85、VCert threshold=43，私网暂估 `70--120 s`、`0.5--1.0 GB/节点`，
公网暂估 `300--900 s` 且可能超时。公网区间可信度低：历史 n=32 公网 MVBA 曾单独放大到约
286 s；在当前代码先通过较小规模 WAN 回归前，不能把上述公网数字写成承诺或论文结果。

### proposer 分工互验证书（正式设计，未实现）

目标是避免每个 sampled proposer 重复验证完整 catalog，同时保持 component ref/lock/root、
payload digest、Pool、ARC、VCert 的原绑定。eligibility coin `E` 通过域分离 PRF 为每个
component 派生至少 `2f+1` 个候选 verifier 的责任窗口；verifier 签署绑定
`epoch/context/E/component_ref_digest/payload_digest/registry_digest/version` 的
`ComponentCheck`。责任窗口达到 `f+1` 有效签名后形成带 signer bitmap/聚合签名和 transcript
digest 的 `ComponentEndorsementCertificate`，proposer 必须证明 selected components 全部
覆盖后才能发布 Pool/VCert。

不能把签名集合限制为 proposer sample：正式 original profile 的 n=96/n=128 proposer sample
为 18/19，而 `f+1` 为 32/43。固定恰好 `f+1` verifier 也会被 Byzantine withholding 破坏活性，
因此要从 `2f+1` 候选开始并按同一 coin 派生扩展窗口；覆盖不足、冲突检查失败、跨 epoch/context
重放均拒绝。责任表摘要、扩展次数和分工失败概率要单独进入 sampling report。

实现顺序：先 codec/签名域/责任覆盖检查，再加 Byzantine withholding、冲突、重放和多 proposer
共用证书测试；保留旧完整验证作为 feature-flag fallback，通过 soundness/liveness 评审后才
做 AWS A/B。当前证据支持分工后约 `32--36 s` 的稳妥目标，`28--32 s` 仍是乐观上限，不应提前
写入论文主表。

### PracticalADKR n=64 ordinary/high-assurance 严格 TCP 对照（2026-08-23）

`high-assumption` 按代码中的实际名称 `high-assurance` 执行。两轮固定 `n=64,f=21`、严格
loopback TCP、Paillier-3072、单 epoch、dealing delta=1000 ms、derive wait-all 关闭、每节点
3 核 affinity，只改变 kappa profile。ordinary 即 `practical-original`，实际 `kappa=20`
（epoch 35.41-bit；525600 epochs union-bound 16.41-bit）；high-assurance 实际
`kappa=22=f+1`，failure probability 为 0。

| profile | 完成 | E2E mean / median / p95 | online median | derive median | sent/recv median |
| --- | ---: | ---: | ---: | ---: | ---: |
| ordinary | 54/64 | 81.85 / 78.32 / 92.07 s | 40.71 s | 4.12 s | 153.75/153.72 MB |
| high-assurance | 63/64 | 190.34 / 190.36 / 190.91 s | 153.59 s | 116.26 s | 155.67/154.66 MB |

两轮均达 quorum=43 且各自只有一个 consensus hash。setup median 为 37.62/36.82 s，MVBA
median 为 29.43/29.48 s，通信量仅约 1% 差异；high-assurance 的非线性放大几乎全部在
CompProve derive。代码没有该 profile 的专用慢分支，`kappa` 的 10% 增量本身不足以解释
derive 约 28 倍差异。优先假设是额外 selected transcripts 的 ACK aux 覆盖触发更多 3072-bit
Paillier fallback，并被 64 进程并发放大；现有日志没有 decrypt count/time，不能视为已证明。
下一步只增加 ACK hit、Paillier decrypt 和 share verify 的 count/time profile 后跑一轮定向验证，
不重复无分项观测的完整 A/B。

首次 ordinary 预跑因误把 `--mem-per-node-mb=3072`（实际为 `RLIMIT_AS`）当 RSS 限制而发生
mmap/pthread OOM，已作废；取消该限制后的两轮没有同类错误。本轮 AWS 新增成本 `$0`。有效产物：
`/tmp/practical-n64-original-r2-20260823/`、
`/tmp/practical-n64-high-assurance-20260823/`。
